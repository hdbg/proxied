[package]
name = "proxied"
version = "0.2.1"
edition = "2021"
description = "Unified proxy connection data structure"
license = "MIT"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
serde = { version = "1.0.202", features = ["derive"] }
thiserror = "1.0.60"
